apiVersion: v1
kind: Service
metadata:
  name: evolution-api
  labels:
    app.kubernetes.io/name: evolution-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: evolution-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
